# Secrets / environment
.env*
!.env.example

# Databases and local DB snapshots
*.db
*.db.bak
*.sqlite
db-backups/

# Stray editor/backup files
*.bak
*.tmp
*.orig

# Runtime data (live DB, uploads, sessions, creator game-data)
runtime/

# Dependencies
node_modules/

# Build output - regenerated by `npm run build` on every Docker build and
# local dev, so it stays out of git.
public/dist/

# Logs and orphan-cleanup trash
*.log
logs/
.trash-*

# OS/editor
.DS_Store
Thumbs.db
.vscode/

# One-off local maintenance scripts (not part of the deployable app)
migrate-uploads.js
update.js

# Kept on the server but not published: deployment/ops, brand assets,
# official game models, and downloaded binaries.
Dockerfile
compose.yaml
.dockerignore
scripts/
tests/
public/downloads/
public/models-official/
public/assets/favicon.ico
public/assets/favicon.svg
public/assets/favicon-16.png
public/assets/favicon-32.png
public/assets/favicon-48.png
public/assets/apple-touch-icon.png
public/assets/ashframe-mark.svg
